# Área X | Comunicação Visual

Landing page simples em React + Vite para a Área X.

## Rodar localmente

```bash
npm install
npm run dev
```

## Publicar na Vercel

Conecte este projeto ao Vercel e use o comando padrão de build:

```bash
npm run build
```

Diretório de saída: `dist`.
